---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install base utilities
  apt:
    name:
      - curl
      - wget
      - unzip
      - vim
    state: present

- name: Install fail2ban
  apt:
    name: fail2ban
    state: present
